Extract `dashboard_image.zip` into this folder to restore the PNG mockups (Updates flow and related screens).
PNG files are gitignored to keep the repo small; the dashboard HTML/CSS follows this visual direction.
See also `js/dashboard/README.md`.
